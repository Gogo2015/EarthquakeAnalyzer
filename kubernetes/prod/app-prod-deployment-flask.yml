---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app
  labels:
    app: flask-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-app
  template:
    metadata:
      labels:
        app: flask-app
    spec:
      containers:
        - name: flask-app
          imagePullPolicy: Always
          image: mitragaurav10/api:1.0
          command: ['python3', 'api.py']  
          ports:
          - name: http
            containerPort: 5000
          env:
          - name: LOG_LEVEL
            value: DEBUG
          - name: REDIS_IP
            value: redis-db-service
